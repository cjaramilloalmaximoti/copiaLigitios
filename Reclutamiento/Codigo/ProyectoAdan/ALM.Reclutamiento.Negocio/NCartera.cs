using ALM.Empresa.Datos;
using ALM.Empresa.Entidades;
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using System.Text;
using System.Net;
using System.IO;

namespace ALM.Empresa.Negocio
{
    public class NCartera
    {
        /*
        public List<ECartera> ObtenerCarteras(string nombre, int activo, int idEmpresa)
        {
            return new DCartera().ObtenerCarteras(nombre, activo, idEmpresa);
        }

        public List<ECartera> ObtenerClientesCarteras(string nombre, int idEmpresa)
        {
            return new DCartera().ObtenerClientesCarteras(nombre, idEmpresa);
        }

        public int InsertarCartera(ECartera cartera, int idIsuarioLog)
        {
            return new DCartera().InsertarCartera(cartera, idIsuarioLog);
        }

        public void ActualizarCartera(ECartera cartera, int idIsuarioLog)
        {
            new DCartera().ActualizarCartera(cartera, idIsuarioLog);
        }
        */
    }
}